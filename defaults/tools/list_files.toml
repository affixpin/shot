healthcheck = "which ls"
name = "list_files"
description = "List files in a directory"
command = 'ls -la "${path:-.}"'

[params.path]
type = "string"
description = "Directory path (default: current directory)"
